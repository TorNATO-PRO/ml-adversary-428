title = "Model Hyperparameters"

# More parameters to be added later
[dataset]
seed = 42
image_width = 256
image_height = 256
validation_split = 0.2
test_split = 0.5

# More parameters to be added later
[model]
seed = 42
batch_size = 20
learning_rate = 0.01
num_epochs = 20
